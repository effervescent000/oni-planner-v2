import type { GenericObject } from "../types/interfaces";
import { todoKeys } from "~/constants/todo-constants";

export const todoMap: GenericObject[] = [
  { key: todoKeys.MEALWOOD },
  { key: todoKeys.MUSH_MASHER },
];
