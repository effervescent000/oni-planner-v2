import type { RouteType } from "./types/interfaces";

const routeMap: RouteType[] = [
  {
    route: "dupePlanner",
    title: "Dupe Manager",
  },
  { route: "todo", title: "Todo List" },
];

export default routeMap;
